spring:
  datasource:
    url: jdbc:mysql://${MYSQL_ADDRESS:127.0.0.1:3306}/${MYSQL_DATABASE:baby_record_test}?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5

  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false

# 微信小程序配置
wechat:
  appid: ${WX_APPID:}
  appsecret: ${WX_APPSECRET:}
  http-verify: ${HTTP_VERIFY:true}
  ca-bundle-path: ${CA_BUNDLE_PATH:}

# 应用配置
app:
  name: 可可宝宝记
  debug: false
  admin-token: ${ADMIN_TOKEN:}

# CORS配置
cors:
  allowed-origins: "*"
  allowed-methods: "*"
  allowed-headers: "*"
  allow-credentials: true
  max-age: 3600

logging:
  level:
    root: INFO
    com.babykeke.backend: INFO
    org.hibernate.SQL: INFO
