-- ================================================
-- 移除头像和文件存储功能的数据库迁移
-- 执行时间: 2025-01-15
-- 说明: 移除 avatar_url 字段和 files 表
-- ================================================

USE `baby_record`;

-- ================================================
-- 1. 删除文件存储表（如果存在）
-- ================================================
DROP TABLE IF EXISTS `files`;

-- ================================================
-- 2. 从 users 表删除 avatar_url 字段
-- ================================================
ALTER TABLE `users` DROP COLUMN IF EXISTS `avatar_url`;

-- ================================================
-- 3. 从 babies 表删除 avatar_url 字段
-- ================================================
ALTER TABLE `babies` DROP COLUMN IF EXISTS `avatar_url`;

-- ================================================
-- 完成信息
-- ================================================
SELECT '✅ 数据库迁移完成：已移除头像和文件存储功能' AS status;
